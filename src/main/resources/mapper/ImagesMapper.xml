<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.travelfamilies.mapper.ImagesMapper">

    <select id="getImages" resultType="com.travelfamilies.pojo.CommentImage">
        SELECT comment_id, image_url
        FROM comment_image
        WHERE comment_id IN
        <foreach collection="commentIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        ORDER BY sort_order ASC
    </select>
</mapper>